#include "config.h"

float velocity = 10;
float factor_separation = 0.5;
float factor_alignment = 0.4;
float factor_cohesion = 0.4;
float factor_intertia = 1;
float radius = 200;
float interaction_radius_2 = radius * radius;
bool animate = true;
bool gpu_render = true;